enum Role {
  CUSTOMER
  TECHNICIAN
  ADMIN
}

enum UserStatus {
  UNBAN
  BAN
}

enum BookingStatus {
  REQUESTED
  ACCEPTED
  DECLINED
  PAID
  COMPLETED
  CANCELLED
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
}

enum PaymentProvider {
  STRIPE
  SSLCOMMERZ
}

enum PaymentMethod {
  CARD
  MOBILE_BANKING
  BANK_TRANSFER
}
